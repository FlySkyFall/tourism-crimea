<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="csrf-token" content="{{csrfToken}}">
  <title>Туры - Краснодарский край</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-100 font-sans">
  {{> navbar}}

  <section class="py-12 bg-white">
    <div class="container mx-auto px-4">
      <h1 class="text-3xl font-bold mb-6">Туры Республики Крым</h1>

      <form id="tour-filter-form" class="mb-8 bg-gray-100 p-6 rounded-lg" onsubmit="return false">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
          <div>
            <label for="type" class="block text-sm font-medium text-gray-700">Тип тура</label>
            <select id="type" name="type" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm">
              <option value="all" {{#if (eq currentType 'all')}}selected{{/if}}>Все</option>
              <option value="active" {{#if (eq currentType 'active')}}selected{{/if}}>Активный</option>
              <option value="passive" {{#if (eq currentType 'passive')}}selected{{/if}}>Пассивный</option>
              <option value="camping" {{#if (eq currentType 'camping')}}selected{{/if}}>Кемпинг</option>
              <option value="excursion" {{#if (eq currentType 'excursion')}}selected{{/if}}>Экскурсионный</option>
              <option value="health" {{#if (eq currentType 'health')}}selected{{/if}}>Оздоровительный</option>
            </select>
          </div>
          <div>
            <label for="search" class="block text-sm font-medium text-gray-700">Поиск</label>
            <input type="text" id="search" name="search" value="{{currentSearch}}" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" placeholder="Название или описание">
          </div>
          <div>
            <label for="region" class="block text-sm font-medium text-gray-700">Регион</label>
            <select id="region" name="region" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm">
              <option value="">Все регионы</option>
              {{#each regions}}
                <option value="{{this.name}}" {{#if (eq ../currentRegion this.name)}}selected{{/if}}>{{this.name}}</option>
              {{/each}}
            </select>
          </div>
          <div>
            <label for="minPrice" class="block text-sm font-medium text-gray-700">Минимальная цена</label>
            <input type="number" id="minPrice" name="minPrice" value="{{currentMinPrice}}" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" placeholder="От">
          </div>
          <div>
            <label for="maxPrice" class="block text-sm font-medium text-gray-700">Максимальная цена</label>
            <input type="number" id="maxPrice" name="maxPrice" value="{{currentMaxPrice}}" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" placeholder="До">
          </div>
          <div>
            <label for="startDate" class="block text-sm font-medium text-gray-700">Дата начала</label>
            <input type="date" id="startDate" name="startDate" value="{{currentStartDate}}" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm">
          </div>
          <div>
            <label for="endDate" class="block text-sm font-medium text-gray-700">Дата окончания</label>
            <input type="date" id="endDate" name="endDate" value="{{currentEndDate}}" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm">
          </div>
          <div>
            <label for="minDuration" class="block text-sm font-medium text-gray-700">Мин. длительность (дни)</label>
            <input type="number" id="minDuration" name="minDuration" value="{{currentMinDuration}}" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" placeholder="От">
          </div>
          <div>
            <label for="maxDuration" class="block text-sm font-medium text-gray-700">Макс. длительность (дни)</label>
            <input type="number" id="maxDuration" name="maxDuration" value="{{currentMaxDuration}}" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" placeholder="До">
          </div>
          <div>
            <label for="amenities" class="block text-sm font-medium text-gray-700">Удобства</label>
            <select id="amenities" name="amenities" multiple class="mt-1 block w-full border-gray-300 rounded-md shadow-sm">
              {{#if amenitiesList}}
                {{#each amenitiesList}}
                  <option value="{{this}}" {{#if (includes ../currentAmenities this)}}selected{{/if}}>{{this}}</option>
                {{/each}}
              {{else}}
                <option value="" disabled>Нет доступных удобств</option>
              {{/if}}
            </select>
          </div>
          <div>
            <label for="sortBy" class="block text-sm font-medium text-gray-700">Сортировка</label>
            <select id="sortBy" name="sortBy" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm">
              <option value="" {{#unless currentSortBy}}selected{{/unless}}>По умолчанию</option>
              <option value="price-asc" {{#if (eq currentSortBy 'price-asc')}}selected{{/if}}>Цена: по возрастанию</option>
              <option value="price-desc" {{#if (eq currentSortBy 'price-desc')}}selected{{/if}}>Цена: по убыванию</option>
              <option value="durationDays-asc" {{#if (eq currentSortBy 'durationDays-asc')}}selected{{/if}}>Длительность: по возрастанию</option>
              <option value="durationDays-desc" {{#if (eq currentSortBy 'durationDays-desc')}}selected{{/if}}>Длительность: по убыванию</option>
            </select>
          </div>
        </div>
        <div class="mt-4 flex justify-end">
          <button type="button" id="filter-btn" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">Применить фильтры</button>
          <button type="button" id="reset-btn" class="ml-2 bg-gray-600 text-white px-4 py-2 rounded-md hover:bg-gray-700">Сбросить</button>
        </div>
      </form>

      {{#if error}}
        <div class="bg-red-100 text-red-700 p-4 rounded-md mb-6">
          {{error}}
        </div>
      {{else}}
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6" data-initial="true">
          {{#each tours}}
            <div class="bg-white rounded-lg shadow-md overflow-hidden relative {{#if isHotDeal}}hot-deal{{/if}}">
              {{#if isHotDeal}}
                <span class="hot-deal-badge">🔥 Горящий тур!</span>
              {{/if}}
              <img src="{{#if images.length}}{{get images 0}}{{else}}/images/default-tour.jpg{{/if}}" alt="{{title}}" class="w-full h-48 object-cover">
              <div class="p-4">
                <h3 class="text-lg font-semibold mb-2">{{title}}</h3>
                <p class="text-gray-600 mb-2">{{location.region}}</p>
                <p class="text-gray-600 mb-2">Длительность: {{durationDays}} дней</p>
                <p class="text-gray-600 mb-2">Тип: {{#if (eq type 'active')}}Активный{{else if (eq type 'passive')}}Пассивный{{else if (eq type 'camping')}}Кемпинг{{else if (eq type 'excursion')}}Экскурсионный{{else if (eq type 'health')}}Оздоровительный{{else}}{{type}}{{/if}}</p>
                <div class="flex items-center mb-2">
                  <span class="text-yellow-500">★ {{rating}}</span>
                  <span class="text-gray-500 ml-2">({{reviewsCount}} отзывов)</span>
                </div>
                <p class="text-lg font-bold text-gray-800">
                  {{#if discounts.groupDiscount.enabled}}
                    <span class="text-green-600">Скидка {{discounts.groupDiscount.percentage}}% за группу от {{discounts.groupDiscount.minParticipants}} чел.</span><br>
                  {{/if}}
                  {{#if discounts.seasonalDiscount.enabled}}
                    <span class="text-green-600">Сезонная скидка {{discounts.seasonalDiscount.percentage}}% до {{formatDate discounts.seasonalDiscount.endDate}}</span><br>
                  {{/if}}
                  {{#if isHotDeal}}
                    <span class="text-green-600">Скидка горящего тура {{discounts.hotDealDiscount.percentage}}%</span><br>
                  {{/if}}
                  Цена: {{price}} ₽ / чел.
                </p>
                <a href="/tours/{{_id}}" class="mt-4 inline-block bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">Подробнее</a>
              </div>
            </div>
          {{/each}}
        </div>

        <div class="mt-8 flex justify-between items-center">
          <p class="text-gray-600">Показано {{toursOnPage}} из {{totalTours}} туров</p>
          <div class="flex space-x-2">
            {{#if (gt currentPage 1)}}
              <a href="/tours?page={{subtract currentPage 1}}{{#if currentType}}&type={{currentType}}{{/if}}{{#if currentSearch}}&search={{currentSearch}}{{/if}}{{#if currentRegion}}&region={{encode currentRegion}}{{/if}}{{#if currentMinPrice}}&minPrice={{currentMinPrice}}{{/if}}{{#if currentMaxPrice}}&maxPrice={{currentMaxPrice}}{{/if}}{{#if currentStartDate}}&startDate={{currentStartDate}}{{/if}}{{#if currentEndDate}}&endDate={{currentEndDate}}{{/if}}{{#if currentSortBy}}&sortBy={{currentSortBy}}{{/if}}{{#if currentMinDuration}}&minDuration={{currentMinDuration}}{{/if}}{{#if currentMaxDuration}}&maxDuration={{currentMaxDuration}}{{/if}}{{#if currentAmenities}}&amenities={{currentAmenities}}{{/if}}" class="px-4 py-2 bg-gray-200 rounded-md">Назад</a>
            {{/if}}
            <span class="px-4 py-2">Страница {{currentPage}} из {{totalPages}}</span>
            {{#if (lt currentPage totalPages)}}
              <a href="/tours?page={{add currentPage 1}}{{#if currentType}}&type={{currentType}}{{/if}}{{#if currentSearch}}&search={{currentSearch}}{{/if}}{{#if currentRegion}}&region={{encode currentRegion}}{{/if}}{{#if currentMinPrice}}&minPrice={{currentMinPrice}}{{/if}}{{#if currentMaxPrice}}&maxPrice={{currentMaxPrice}}{{/if}}{{#if currentStartDate}}&startDate={{currentStartDate}}{{/if}}{{#if currentEndDate}}&endDate={{currentEndDate}}{{/if}}{{#if currentSortBy}}&sortBy={{currentSortBy}}{{/if}}{{#if currentMinDuration}}&minDuration={{currentMinDuration}}{{/if}}{{#if currentMaxDuration}}&maxDuration={{currentMaxDuration}}{{/if}}{{#if currentAmenities}}&amenities={{currentAmenities}}{{/if}}" class="px-4 py-2 bg-gray-200 rounded-md">Вперёд</a>
            {{/if}}
          </div>
        </div>
      {{/if}}
    </div>
  </section>

  <script src="/js/tours.js"></script>
</body>
</html>